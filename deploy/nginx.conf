# Nginx 反向代理配置 - Jev Agent Routing
# 用法：把这个文件放到服务器 /etc/nginx/conf.d/jev.conf
# 然后 nginx -t && nginx -s reload

server {
    listen 80;
    server_name taoxie.vip www.taoxie.vip;

    # 静态资源直接返回（前端单页）
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # API 接口
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 60s;
    }

    # 健康检查
    location /health {
        proxy_pass http://127.0.0.1:8000/;
        access_log off;
    }

    # 上传大小限制
    client_max_body_size 10M;

    # 开启 gzip
    gzip on;
    gzip_types text/plain application/json text/css application/javascript;
    gzip_min_length 1000;
}

# HTTPS 配置（有证书后取消注释）
# server {
#     listen 443 ssl http2;
#     server_name taoxie.vip www.taoxie.vip;
#
#     ssl_certificate /etc/letsencrypt/live/taoxie.vip/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/taoxie.vip/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
